# Seals

This public repository intentionally does not include real company seals.

For local/private use, add seal images referenced by `assets/config/company-profile.example.json`, then rename the config to a private local filename such as:

```text
assets/config/company-profile.local.json
```

Do not commit real seal images to a public repository.
